import {Person} from './types'

export const calc = (number1: number, number2: number): number => {
    return number1 + number2
}

export const calc2 = (test : Person): number => {
    return 1
}

